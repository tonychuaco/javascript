import React from 'react';

const Hoisting = () => 
<section>
  <h2>Hoisting</h2>
  <ul>
	<li><i>Hoisting</i> is JavaScript's default behavior of moving declarations to the top.</li>
	<li>JavaScript Declarations are Hoisted</li>
	<li>Variables and constants declared with <code>let</code> or <code>const</code> are not hoisted.</li>
	<li>JavaScript Initializations are also not hoisted.</li>
	<li>Good practice is still to declare variables at the top.</li>
</ul>
</section>

export default Hoisting;