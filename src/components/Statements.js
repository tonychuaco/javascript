import React from 'react';

const Statements = () => 
<section>
  <h2>Statements</h2>
	<ul>
		<li>JS <b>Statement</b>: is each line of JavaScript, which essentially refers to a line of code</li>
		<li>JS Programs: refers to the entire Codes, which is essentially a list of JS Statements</li>
		<li>Semicolons ( ; ): ends a statement</li>
		<li>White Space: is ignored</li>
		<li>Line Length and Line Breaks: is ignored</li>
		<li><b>Code Blocks</b>: refers to all statements inside brackets {...}</li>
	</ul>
</section>

export default Statements;